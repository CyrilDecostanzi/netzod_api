/**
 * Enum for post status values.
 * @readonly
 * @enum {number} - The post status values (ACTIVE = 1, AWAITING_APPROVAL = 2, INACTIVE = 0, DRAFT = 3)
 * @export PostStatus
 */
export enum PostStatus {
  ACTIVE = 1,
  AWAITING_APPROVAL = 2,
  INACTIVE = 0,
  DRAFT = 3,
}
